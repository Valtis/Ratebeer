<h2>Top three breweries</h2>
<%= render 'list', items: @breweries, name: "Beer" %>

<h2>Top three beers</h2>
<%= render 'list', items: @beers, name: "Beer" %>


<h2>Top three styles</h2>
<%= render 'list', items: @styles, name: "Style" %>

<h2>Top three raters</h2>
<%= render 'list', items: @users, name: "User" %>

<h2>Recent ratings</h2>
<table class="table table-hover">
  <thead>
  <tr>
    <th>Beer name</th>
    <th>Rater</th>
    <th>Score</th>
  </tr>
  </thead>

  <tbody>

  <% @recent_ratings.each do |rating| %>
      <tr>
        <th> <%= link_to rating.beer.name, beer_path(rating.beer) %></th>
        <th> <%= link_to rating.user.username, user_path(rating.user)%></th>
        <th><%= rating.score %> </th>
      </tr>
  <% end %>
  </tbody>
</table>